server:
  port: 9002
  
# actuator
#http://localhost:9002/actuator
management:
  endpoints:
    web:
      exposure:
        include: "*"

# 打印log日志
logging:
  level:
    root: warn
    org.springframework.cloud: debug
